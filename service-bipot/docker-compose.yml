# Compose file milik service-bipot sendiri — folder top-level, sejajar dengan traefik/,
# auth-service/, mariadb/ (dulu ada di services/service-bipot/, sudah dipindah ke root lewat
# git mv supaya tiap service benar-benar berdiri sendiri, bukan lagi bernaung di bawah folder
# services/ bersama). Network `gateway-net` harus sudah dibuat manual (nama-nya dipin literal
# supaya bisa dirujuk di sini).
#
# DATABASE_URL_SIMAKU dan DATABASE_URL_SIADE menunjuk ke database eksternal (BUKAN mariadb
# gateway) — kredensial harus disediakan sendiri, lihat .env.example.
#
# Jalankan: docker compose up -d --build   (dari folder ini, SETELAH auth-service jalan)

networks:
  gateway-net:
    name: gateway-net
    external: true

services:
  service-bipot:
    build: .
    container_name: service-bipot
    environment:
      DATABASE_URL_SIMAKU: ${DATABASE_URL_SIMAKU}
      DATABASE_URL_SIADE: ${DATABASE_URL_SIADE}
      PORT: 5000
      # Budget koneksi: total DB_POOL_SIZE semua service yang nempel ke database yang
      # sama harus punya headroom di bawah DB_MAX_CONNECTIONS-nya — lihat CLAUDE.md
      # bagian "Kapasitas Database".
      DB_POOL_SIZE: ${DB_POOL_SIZE:-10}
    networks:
      - gateway-net
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    mem_limit: ${SERVICE_MEM_LIMIT:-256m}
    cpus: ${SERVICE_CPUS:-0.5}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/health"]
      interval: 15s
      timeout: 3s
      retries: 3
